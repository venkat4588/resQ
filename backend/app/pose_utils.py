def analyze_pose(frame):
    """
    Extract pose-related features.
    Replace this dummy with actual OpenPose/MediaPipe logic as needed.
    Returns a fixed-length list (e.g. [0.0, 0.0]) if no pose lib available.
    """
    # TODO: Integrate MediaPipe/OpenPose for real features
    return [0.0, 0.0]
